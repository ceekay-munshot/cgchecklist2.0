@AGENTS.md

## Project: CG Checklist (read this first)

ALWAYS read `PROJECT_BRIEF.md` at the repo root before starting any work in this
repo. It is the source of truth for the product, architecture, folder layout,
the LLM/scrape provider-routing table, env vars, and conventions every session
must follow.

This project runs on a customized **Next.js 16** (App Router). As `AGENTS.md`
states, it is NOT the Next.js you know — read the relevant guide under
`node_modules/next/dist/docs/` and heed deprecation notices before writing any
Next.js code.
